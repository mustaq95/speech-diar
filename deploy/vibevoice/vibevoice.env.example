# Copy to deploy/vibevoice/vibevoice.env (gitignored) and adjust.
#
# No token needed: microsoft/VibeVoice-ASR-HF is MIT-licensed and ungated,
# unlike the pyannote community-1 weights this platform gates behind
# HUGGINGFACE_TOKEN.

# Host port (change if 9023 is taken)
VIBEVOICE_PORT=9023

# The transformers-native repo. Do not point this at microsoft/VibeVoice-ASR:
# that one carries the original research codebase's checkpoint format, not the
# VibeVoiceAsrForConditionalGeneration architecture that transformers loads.
VIBEVOICE_MODEL_ID=microsoft/VibeVoice-ASR-HF
