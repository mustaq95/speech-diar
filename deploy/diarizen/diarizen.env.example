# Copy to deploy/diarizen/diarizen.env (gitignored) and adjust.
#
# Note: DiariZen's pretrained weights are CC BY-NC 4.0 (non-commercial /
# research use only) -- keep that in mind if this platform's use ever
# changes.

# Host port (change if 9022 is taken)
DIARIZEN_PORT=9022
